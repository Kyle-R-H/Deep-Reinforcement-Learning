# Classic Control

# Why Reinforcement Learning is the machine learning paradigm of choice for this task? (2 Marks)
# Cause it good at Generalisation with working with random environments....

import torch
import gymnasium


device = torch.device("cuda" if torch.cuda.is_available() else "cpu")